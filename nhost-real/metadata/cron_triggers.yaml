- name: scheduled_workflow_check
  webhook: "{{NHOST_BACKEND_URL}}/v1/functions/scheduled-trigger"
  schedule: "*/5 * * * *"
  include_in_metadata: true
  payload: {}
  retry_conf:
    num_retries: 1
    retry_interval_seconds: 10
    timeout_seconds: 60
